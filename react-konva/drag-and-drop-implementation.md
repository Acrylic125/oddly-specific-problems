# Implement a drag-and-drop feature for a Konva element.
## Solution
1. Use `draggable` property to make the Konva element draggable.
1. On `onDragStart`, and `onDragStart`, capture the start and end position by using `event.target.getPosition()`.
